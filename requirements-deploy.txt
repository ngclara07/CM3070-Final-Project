# =============================================================================
# SenseFuzeAI - Production / Cloud Deployment Requirements
# Python 3.11
# =============================================================================

# CPU-only PyTorch package index for cloud deployment
--extra-index-url https://download.pytorch.org/whl/cpu

# -----------------------------------------------------------------------------
# FastAPI web application
# -----------------------------------------------------------------------------
fastapi==0.136.0
uvicorn[standard]==0.44.0
python-multipart==0.0.26
jinja2==3.1.6
websockets==16.0

# -----------------------------------------------------------------------------
# Core numerical / ML runtime
# -----------------------------------------------------------------------------
numpy==2.1.3
pandas==3.0.2
scipy==1.17.1
joblib==1.5.3
scikit-learn==1.8.0

# -----------------------------------------------------------------------------
# Image processing
# -----------------------------------------------------------------------------
Pillow==12.2.0
opencv-python-headless==4.13.0.92

# -----------------------------------------------------------------------------
# Audio processing
# -----------------------------------------------------------------------------
librosa==0.11.0
soundfile==0.13.1
soxr==1.0.0
numba==0.65.0

# -----------------------------------------------------------------------------
# Hugging Face / multimodal neural models
# MPNet + WavLM + CLIP
# -----------------------------------------------------------------------------
transformers==5.5.4
sentence-transformers==5.5.1
safetensors==0.7.0
huggingface-hub==1.11.0
tokenizers==0.22.2

# -----------------------------------------------------------------------------
# PyTorch
# Required by MPNet, WavLM and CLIP inference.
# -----------------------------------------------------------------------------
torch==2.11.0
torchvision==0.26.0

# -----------------------------------------------------------------------------
# Possible serialized fusion / webcam classifier backends.
#
# Retained because joblib models must have their original estimator libraries
# available when deserialized. These can be reduced further once the exact
# estimator classes in the saved .joblib artifacts are confirmed.
# -----------------------------------------------------------------------------
catboost==1.2.10
lightgbm==4.6.0
xgboost==3.2.0

# -----------------------------------------------------------------------------
# General runtime support
# -----------------------------------------------------------------------------
requests==2.33.1
python-dotenv==1.2.2
PyYAML==6.0.3
